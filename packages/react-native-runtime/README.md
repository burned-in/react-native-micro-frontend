# @bunin/react-native-micro-frontend-runtime

Part of the `@bunin/react-native-micro-frontend` monorepo.

This package is published from `dist/` after `bun run build`.

See the root README for CLI examples, native contract policy, and OTA rules.

Development status: active development on the 0.x line; pin exact versions before production use.

License: MIT.
