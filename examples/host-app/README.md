# Legacy host fixture

This folder is kept as the original compact host fixture. For transport-specific examples, use the split folders instead:

- `../general-ts` — normal TypeScript module through Metro and `withMfe`
- `../bundle` — compressed bundle archive without OTA publish
- `../ota` — Hot Updater/custom OTA delivery after verification
